<%= form_with model: discord_recipient do |form| %>
  <div class="form-floating mb-3">
    <%= form.text_field :channel_id, class: ["form-control"]%>
    <%= form.label :channel_id, "チャンネル ID" %>
    <% discord_recipient.errors.messages_for(:url).each do |message| %>
      <p class="text-danger"><%= message %></p>
    <% end %>
  </div>
  <div class="form-floating mb-3">
    <p class="mb-2">タグ</p>
    <%= form.collection_check_boxes :tag_ids, Tag.all, :id, :name do |b| %>
      <div class="form-check form-check-inline">
        <%= b.check_box class: ["form-check-input"] %>
        <%= b.label class: ["form-check-label"] %>
      </div>
    <% end %>
  </div>
  <div class="actions mb-3">
    <%= form.submit %>
  </div>
<% end %>
